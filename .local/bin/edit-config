#!/usr/bin/bash

CONFIG_FILES=$HOME/.config/scripts/config-files

choice=$(awk '{print $1}' "$CONFIG_FILES" | dmenu -i -l 10 -c -f -p 'edit config:')

if [ "$choice" ]; then
    cfg=$(grep "^$choice " "$CONFIG_FILES" | awk '{print $NF}')
    gnome-terminal -- "$EDITOR" "$HOME/$cfg"
fi
